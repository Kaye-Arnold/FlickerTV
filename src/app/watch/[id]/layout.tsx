import { ReactNode } from 'react';

export const metadata = {
  title: 'Watch | Flicker TV',
  description: 'Watch video',
};

export default function WatchLayout({ children }: { children: ReactNode }) {
  return children;
}
